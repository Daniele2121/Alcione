name: iOS TestFlight Build
on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  deploy:
    name: Build and Deploy
    runs-on: macos-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: Install Apple Certificate
        uses: apple-actions/import-codesign-certs@v3
        with:
          p12-file-base64: ${{ secrets.P12_BASE64 }}
          p12-password: ${{ secrets.P12_PASSWORD }}
          create-keychain: true

      - name: Install Provisioning Profile
        run: |
          mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
          echo "${{ secrets.PROVISIONING_BASE64 }}" | base64 --decode > ~/Library/MobileDevice/Provisioning\ Profiles/Alcione_TestFlight_2026.mobileprovision

      - name: Build and Export IPA
        run: |
          flutter pub get
          cd ios
          rm -rf Pods
          rm -f Podfile.lock
          pod install --repo-update
          cd ..
          
          # 1. Archiviazione
          flutter build ipa --release --no-codesign --build-number=$GITHUB_RUN_NUMBER

          # 2. PULIZIA CHIRURGICA DELLA STRUTTURA (Soluzione Errore 409)
          # Entriamo nell'archivio e puliamo App.framework
          FRAMEWORK_PATH="build/ios/archive/Runner.xcarchive/Products/Applications/Runner.app/Frameworks/App.framework"
          
          echo "Pulizia profonda della struttura framework..."
          # Rimuove file binari duplicati o non necessari che mandano in tilt altool
          rm -rf "$FRAMEWORK_PATH/App"
          rm -rf "$FRAMEWORK_PATH/_CodeSignature"
          
          # 3. Export IPA (Usando il comando xcodebuild per avere più controllo)
          mkdir -p build/ios/testflight
          xcodebuild -exportArchive \
            -archivePath build/ios/archive/Runner.xcarchive \
            -exportPath build/ios/testflight \
            -exportOptionsPlist ios/ExportOptions.plist \
            CODE_SIGNING_ALLOWED=NO
          
          # 4. Rename per lo step di upload
          mv build/ios/testflight/*.ipa build/ios/testflight/Runner.ipa

      - name: Upload to TestFlight
        env:
          APPSTORE_ISSUER_ID: ${{ secrets.APPSTORE_ISSUER_ID }}
          APPSTORE_KEY_ID: ${{ secrets.APPSTORE_KEY_ID }}
          APPSTORE_P8_TEXT: ${{ secrets.APPSTORE_P8_KEY }}
        run: |
          mkdir -p ~/.appstoreconnect/private_keys
          echo "$APPSTORE_P8_TEXT" > ~/.appstoreconnect/private_keys/AuthKey_$APPSTORE_KEY_ID.p8
          
          xcrun altool --upload-app --type ios \
            -f build/ios/testflight/Runner.ipa \
            --apiKey "$APPSTORE_KEY_ID" \
            --apiIssuer "$APPSTORE_ISSUER_ID"