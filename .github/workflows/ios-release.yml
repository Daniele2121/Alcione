name: iOS TestFlight Build
on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  deploy:
    name: Build and Deploy
    runs-on: macos-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: Install Apple Certificate
        uses: apple-actions/import-codesign-certs@v3
        with:
          p12-file-base64: ${{ secrets.P12_BASE64 }}
          p12-password: ${{ secrets.P12_PASSWORD }}
          create-keychain: true

      - name: Install Provisioning Profile
        run: |
          mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
          echo "${{ secrets.PROVISIONING_BASE64 }}" | base64 --decode > ~/Library/MobileDevice/Provisioning\ Profiles/Alcione_TestFlight_2026.mobileprovision

      - name: Build IPA
        run: |
            flutter pub get
            cd ios
            pod install --repo-update
            cd ..
            # Creiamo una cartella specifica per l'output
            mkdir -p build/ios/testflight
            # Build forzando il percorso di uscita
            flutter build ipa --release \
            --export-options-plist=ios/ExportOptions.plist \
            --build-number=$GITHUB_RUN_NUMBER \
            --output-dir=build/ios/testflight

      - name: Upload to TestFlight
        env:
              APPSTORE_ISSUER_ID: ${{ secrets.APPSTORE_ISSUER_ID }}
              APPSTORE_KEY_ID: ${{ secrets.APPSTORE_KEY_ID }}
              APPSTORE_P8_TEXT: ${{ secrets.APPSTORE_P8_KEY }}
        run: |
              # 1. Configura la chiave
              mkdir -p ~/.appstoreconnect/private_keys
              echo "$APPSTORE_P8_TEXT" > ~/.appstoreconnect/private_keys/AuthKey_$APPSTORE_KEY_ID.p8
                
              # 2. Il percorso ora è CERTO perché lo abbiamo forzato sopra
              IPA_PATH="build/ios/testflight/Runner.ipa"                echo "Caricamento del file: $IPA_PATH"
                
               # 3. Caricamento
               xcrun altool --upload-app --type ios \
                -f "$IPA_PATH" \
                --apiKey "$APPSTORE_KEY_ID" \
                --apiIssuer "$APPSTORE_ISSUER_ID"