name: iOS TestFlight Build
on:
  workflow_dispatch: # Permette l'avvio manuale da GitHub Actions

jobs:
  deploy:
    name: Build and Deploy to TestFlight
    runs-on: macos-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install Apple Certificate
        env:
          CERTIFICATE_CER: ${{ secrets.IOS_CERTIFICATE_CER }}
          PRIVATE_KEY: ${{ secrets.IOS_PRIVATE_KEY }}
          P12_PASSWORD: ${{ secrets.P12_PASSWORD }}
        run: |
          # Decodifica sicura del certificato .cer
          cat <<EOF | tr -d '\r\n ' | base64 --decode > distribution.cer
          $CERTIFICATE_CER
          EOF
          
          # Decodifica sicura della chiave privata .key
          cat <<EOF | tr -d '\r\n ' | base64 --decode > private.key
          $PRIVATE_KEY
          EOF
          
          # Creazione del file P12 direttamente sul Mac
          openssl pkcs12 -export -out certificate.p12 -inkey private.key -in distribution.cer -passout pass:$P12_PASSWORD
          
          # Configurazione del portachiavi (keychain) del Mac
          security create-keychain -p "" build.keychain
          security import certificate.p12 -k build.keychain -P "$P12_PASSWORD" -T /usr/bin/codesign
          security list-keychains -d user -s build.keychain
          security set-key-partition-list -S apple-tool:,apple: -s -k "" build.keychain

      - name: Install Provisioning Profile
        env:
          PROVISIONING_PROFILE: ${{ secrets.PROVISIONING_PROFILE }}
        run: |
          mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
          cat <<EOF | tr -d '\r\n ' | base64 --decode > ~/Library/MobileDevice/Provisioning\ Profiles/profile.mobileprovision
          $PROVISIONING_PROFILE
          EOF

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Install dependencies
        run: flutter pub get

      - name: Build IPA
        run: flutter build ipa --release --export-options-plist=ios/ExportOptions.plist

      - name: Upload to TestFlight
        env:
          APPSTORE_ISSUER_ID: ${{ secrets.APPSTORE_ISSUER_ID }}
          APPSTORE_KEY_ID: ${{ secrets.APPSTORE_KEY_ID }}
          APPSTORE_P8_KEY: ${{ secrets.APPSTORE_P8_KEY }}
        run: |
          # Crea il file della chiave .p8 temporaneo per l'upload
          echo "$APPSTORE_P8_KEY" > auth_key.p8
          
          # Carica l'IPA su TestFlight
          xcrun altool --upload-app --type ios -f build/ios/ipa/*.ipa --apiKey "$APPSTORE_KEY_ID" --apiIssuer "$APPSTORE_ISSUER_ID"