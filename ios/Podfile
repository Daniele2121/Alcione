platform :ios, '15.0'

podhelper_path = File.join(File.dirname(__FILE__), 'Flutter', 'podhelper.rb')
require podhelper_path

target 'Runner' do
  use_frameworks!
  use_modular_headers!

  # Forza Xcode a vedere i plugin come moduli statici
  pod 'Firebase/Firestore'
  pod 'cloud_firestore', :path => '.symlinks/plugins/cloud_firestore/ios'
  pod 'firebase_core', :path => '.symlinks/plugins/firebase_core/ios'
  pod 'firebase_auth', :path => '.symlinks/plugins/firebase_auth/ios'

  flutter_install_all_ios_pods File.dirname(__FILE__)
end

post_install do |installer|
  installer.pods_project.targets.each do |target|
    flutter_additional_ios_build_settings(target)
    target.build_configurations.each do |config|
      config.build_settings['IPHONEOS_DEPLOYMENT_TARGET'] = '15.0'
      config.build_settings['ENABLE_BITCODE'] = 'NO'
    end
  end
end