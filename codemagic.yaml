workflows:
  ios-testflight-workflow:
    name: iOS TestFlight Build
    max_build_duration: 120
    instance_type: mac_mini_m1

    integrations:
      app_store_connect: "Chiave Alcione"

    environment:
      flutter: stable
      xcode: latest
      groups:
        - app_store_credentials

    scripts:
      - name: Flutter Clean and Get
        script: |
          flutter clean
          flutter pub get
          flutter precache --ios
      - name: Install Pods
        script: |
          cd ios
          rm -rf Pods
          rm -rf Podfile.lock
          pod install --repo-update
          cd ..
      - name: Flutter Build iOS
        script: |
          # Genera la configurazione e i file ponte
          flutter build ios --config-only
          touch ios/Runner/GeneratedPluginRegistrant.h
          touch ios/Runner/GeneratedPluginRegistrant.m
          
          # Build finale (usa --no-codesign qui se la firma la fa Codemagic dopo)
          flutter build ios --release --no-codesign --build-number=$BUILD_NUMBER
    artifacts:
      - build/ios/iphoneos/*.app
      - build/ios/ipa/*.ipa

    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true